#!/bin/sh
# Auto-rebuild dist/ when src/ files change

if git diff --cached --name-only --diff-filter=ACMR | grep -qE '^src/|^package\.json|^package-lock\.json'; then
  echo "🔨 Rebuilding dist/..."
  npm run package || exit 1
  git add dist/index.js dist/index.js.map 2>/dev/null
fi
